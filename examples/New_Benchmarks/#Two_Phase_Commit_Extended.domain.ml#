let (chan_A_B : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_A_C : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_A_Coordinator : string Domainslib.Chan.t) =
  Domainslib.Chan.make_bounded 0
let (chan_A_D : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_A_E : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_A_F : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_B_A : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_B_C : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_B_Coordinator : string Domainslib.Chan.t) =
  Domainslib.Chan.make_bounded 0
let (chan_B_D : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_B_E : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_B_F : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_C_A : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_C_B : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_C_Coordinator : string Domainslib.Chan.t) =
  Domainslib.Chan.make_bounded 0
let (chan_C_D : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_C_E : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_C_F : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_Coordinator_A : string Domainslib.Chan.t) =
  Domainslib.Chan.make_bounded 0
let (chan_Coordinator_B : string Domainslib.Chan.t) =
  Domainslib.Chan.make_bounded 0
let (chan_Coordinator_C : string Domainslib.Chan.t) =
  Domainslib.Chan.make_bounded 0
let (chan_Coordinator_D : string Domainslib.Chan.t) =
  Domainslib.Chan.make_bounded 0
let (chan_Coordinator_E : string Domainslib.Chan.t) =
  Domainslib.Chan.make_bounded 0
let (chan_Coordinator_F : string Domainslib.Chan.t) =
  Domainslib.Chan.make_bounded 0
let (chan_D_A : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_D_B : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_D_C : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_D_Coordinator : string Domainslib.Chan.t) =
  Domainslib.Chan.make_bounded 0
let (chan_D_E : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_D_F : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_E_A : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_E_B : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_E_C : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_E_Coordinator : string Domainslib.Chan.t) =
  Domainslib.Chan.make_bounded 0
let (chan_E_D : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_E_F : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_F_A : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_F_B : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_F_C : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_F_Coordinator : string Domainslib.Chan.t) =
  Domainslib.Chan.make_bounded 0
let (chan_F_D : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
let (chan_F_E : string Domainslib.Chan.t) = Domainslib.Chan.make_bounded 0
;;let domain_A =
    Domain.spawn
      (fun _ ->
         let rec gettimeofday arg = Unix.gettimeofday arg in
         let rec print_float arg = Stdlib.print_float arg in
         let rec sub_float arg = Stdlib.(-.) arg in
         let rec get_mod arg = Stdlib.(mod) arg in
         let rec assent_A =
           let rec x = 10 in
           let rec msg_ask =
             Marshal.from_string (Domainslib.Chan.recv chan_Coordinator_A) 0 in
           let rec msg_ask_intermed_result = (get_mod x) msg_ask in
           if msg_ask_intermed_result = 0
           then
             (Domainslib.Chan.send chan_A_Coordinator "L";
              (let rec msg_ask_res = 1 in msg_ask_res))
           else
             (Domainslib.Chan.send chan_A_Coordinator "R";
              (let rec msg_ask_res = 0 in msg_ask_res)) in
         let rec assent_B = () in
         let rec assent_C = () in
         let rec assent_D = () in
         let rec assent_E = () in
         let rec assent_F = () in
         let rec commit_A = let rec commit_val = 5 in commit_val in
         let rec commit_B = () in
         let rec commit_C = () in
         let rec commit_D = () in
         let rec commit_E = () in
         let rec commit_F = () in
         let rec abort_A = let rec abort_val = () in abort_val in
         let rec abort_B = () in
         let rec abort_C = () in
         let rec abort_D = () in
         let rec abort_E = () in
         let rec abort_F = () in
         let rec two_phase_commit =
           let rec assent_reply_A = assent_A in
           let rec _unit_6 =
             let val_5 = assent_reply_A in
             Domainslib.Chan.send chan_A_Coordinator
               (Marshal.to_string val_5 []) in
           match Domainslib.Chan.recv chan_Coordinator_A with
           | "COMMIT" ->
               let rec commit_val = commit_A in
               let rec _unit_2 =
                 let val_1 = commit_val in
                 Domainslib.Chan.send chan_A_Coordinator
                   (Marshal.to_string val_1 []) in
               ()
           | "ABORT" ->
               let rec abort_val = abort_A in
               let rec _unit_4 =
                 let val_3 = abort_val in
                 Domainslib.Chan.send chan_A_Coordinator
                   (Marshal.to_string val_3 []) in
               ()
           | _ -> failwith "Runtime Error: Unmatched label" in
         let rec _unit_7 = two_phase_commit in ())
  and domain_B =
    Domain.spawn
      (fun _ ->
         let rec gettimeofday arg = Unix.gettimeofday arg in
         let rec print_float arg = Stdlib.print_float arg in
         let rec sub_float arg = Stdlib.(-.) arg in
         let rec get_mod arg = Stdlib.(mod) arg in
         let rec assent_A = () in
         let rec assent_B =
           let rec x = 10 in
           let rec msg_ask =
             Marshal.from_string (Domainslib.Chan.recv chan_Coordinator_B) 0 in
           let rec msg_ask_intermed_result = (get_mod x) msg_ask in
           if msg_ask_intermed_result = 0
           then
             (Domainslib.Chan.send chan_B_Coordinator "L";
              (let rec msg_ask_res = 1 in msg_ask_res))
           else
             (Domainslib.Chan.send chan_B_Coordinator "R";
              (let rec msg_ask_res = 0 in msg_ask_res)) in
         let rec assent_C = () in
         let rec assent_D = () in
         let rec assent_E = () in
         let rec assent_F = () in
         let rec commit_A = () in
         let rec commit_B = let rec commit_val = 5 in commit_val in
         let rec commit_C = () in
         let rec commit_D = () in
         let rec commit_E = () in
         let rec commit_F = () in
         let rec abort_A = () in
         let rec abort_B = let rec abort_val = () in abort_val in
         let rec abort_C = () in
         let rec abort_D = () in
         let rec abort_E = () in
         let rec abort_F = () in
         let rec two_phase_commit =
           let rec assent_reply_B = assent_B in
           let rec _unit_13 =
             let val_12 = assent_reply_B in
             Domainslib.Chan.send chan_B_Coordinator
               (Marshal.to_string val_12 []) in
           match Domainslib.Chan.recv chan_Coordinator_B with
           | "ABORT" ->
               let rec abort_val = abort_B in
               let rec _unit_9 =
                 let val_8 = abort_val in
                 Domainslib.Chan.send chan_B_Coordinator
                   (Marshal.to_string val_8 []) in
               ()
           | "COMMIT" ->
               let rec commit_val = commit_B in
               let rec _unit_11 =
                 let val_10 = commit_val in
                 Domainslib.Chan.send chan_B_Coordinator
                   (Marshal.to_string val_10 []) in
               ()
           | _ -> failwith "Runtime Error: Unmatched label" in
         let rec _unit_14 = two_phase_commit in ())
  and domain_C =
    Domain.spawn
      (fun _ ->
         let rec gettimeofday arg = Unix.gettimeofday arg in
         let rec print_float arg = Stdlib.print_float arg in
         let rec sub_float arg = Stdlib.(-.) arg in
         let rec get_mod arg = Stdlib.(mod) arg in
         let rec assent_A = () in
         let rec assent_B = () in
         let rec assent_C =
           let rec x = 10 in
           let rec msg_ask =
             Marshal.from_string (Domainslib.Chan.recv chan_Coordinator_C) 0 in
           let rec msg_ask_intermed_result = (get_mod x) msg_ask in
           if msg_ask_intermed_result = 0
           then
             (Domainslib.Chan.send chan_C_Coordinator "L";
              (let rec msg_ask_res = 1 in msg_ask_res))
           else
             (Domainslib.Chan.send chan_C_Coordinator "R";
              (let rec msg_ask_res = 0 in msg_ask_res)) in
         let rec assent_D = () in
         let rec assent_E = () in
         let rec assent_F = () in
         let rec commit_A = () in
         let rec commit_B = () in
         let rec commit_C = let rec commit_val = 5 in commit_val in
         let rec commit_D = () in
         let rec commit_E = () in
         let rec commit_F = () in
         let rec abort_A = () in
         let rec abort_B = () in
         let rec abort_C = let rec abort_val = () in abort_val in
         let rec abort_D = () in
         let rec abort_E = () in
         let rec abort_F = () in
         let rec two_phase_commit =
           let rec assent_reply_C = assent_C in
           let rec _unit_20 =
             let val_19 = assent_reply_C in
             Domainslib.Chan.send chan_C_Coordinator
               (Marshal.to_string val_19 []) in
           match Domainslib.Chan.recv chan_Coordinator_C with
           | "ABORT" ->
               let rec abort_val = abort_C in
               let rec _unit_16 =
                 let val_15 = abort_val in
                 Domainslib.Chan.send chan_C_Coordinator
                   (Marshal.to_string val_15 []) in
               ()
           | "COMMIT" ->
               let rec commit_val = commit_C in
               let rec _unit_18 =
                 let val_17 = commit_val in
                 Domainslib.Chan.send chan_C_Coordinator
                   (Marshal.to_string val_17 []) in
               ()
           | _ -> failwith "Runtime Error: Unmatched label" in
         let rec _unit_21 = two_phase_commit in ())
  and domain_Coordinator =
    Domain.spawn
      (fun _ ->
         let rec gettimeofday arg = Unix.gettimeofday arg in
         let rec print_float arg = Stdlib.print_float arg in
         let rec sub_float arg = Stdlib.(-.) arg in
         let rec get_mod arg = Stdlib.(mod) arg in
         let rec assent_A =
           let rec msg_ask = 2 in
           let rec _unit_23 =
             let val_22 = msg_ask in
             Domainslib.Chan.send chan_Coordinator_A
               (Marshal.to_string val_22 []) in
           match Domainslib.Chan.recv chan_A_Coordinator with
           | "R" -> ()
           | "L" -> ()
           | _ -> failwith "Runtime Error: Unmatched label" in
         let rec assent_B =
           let rec msg_ask = 2 in
           let rec _unit_25 =
             let val_24 = msg_ask in
             Domainslib.Chan.send chan_Coordinator_B
               (Marshal.to_string val_24 []) in
           match Domainslib.Chan.recv chan_B_Coordinator with
           | "L" -> ()
           | "R" -> ()
           | _ -> failwith "Runtime Error: Unmatched label" in
         let rec assent_C =
           let rec msg_ask = 2 in
           let rec _unit_27 =
             let val_26 = msg_ask in
             Domainslib.Chan.send chan_Coordinator_C
               (Marshal.to_string val_26 []) in
           match Domainslib.Chan.recv chan_C_Coordinator with
           | "R" -> ()
           | "L" -> ()
           | _ -> failwith "Runtime Error: Unmatched label" in
         let rec assent_D =
           let rec msg_ask = 2 in
           let rec _unit_29 =
             let val_28 = msg_ask in
             Domainslib.Chan.send chan_Coordinator_D
               (Marshal.to_string val_28 []) in
           match Domainslib.Chan.recv chan_D_Coordinator with
           | "L" -> ()
           | "R" -> ()
           | _ -> failwith "Runtime Error: Unmatched label" in
         let rec assent_E =
           let rec msg_ask = 2 in
           let rec _unit_31 =
             let val_30 = msg_ask in
             Domainslib.Chan.send chan_Coordinator_E
               (Marshal.to_string val_30 []) in
           match Domainslib.Chan.recv chan_E_Coordinator with
           | "L" -> ()
           | "R" -> ()
           | _ -> failwith "Runtime Error: Unmatched label" in
         let rec assent_F =
           let rec msg_ask = 2 in
           let rec _unit_33 =
             let val_32 = msg_ask in
             Domainslib.Chan.send chan_Coordinator_F
               (Marshal.to_string val_32 []) in
           match Domainslib.Chan.recv chan_F_Coordinator with
           | "L" -> ()
           | "R" -> ()
           | _ -> failwith "Runtime Error: Unmatched label" in
         let rec commit_A = () in
         let rec commit_B = () in
         let rec commit_C = () in
         let rec commit_D = () in
         let rec commit_E = () in
         let rec commit_F = () in
         let rec abort_A = () in
         let rec abort_B = () in
         let rec abort_C = () in
         let rec abort_D = () in
         let rec abort_E = () in
         let rec abort_F = () in
         let rec two_phase_commit =
           let rec assent_reply_A =
             Marshal.from_string (Domainslib.Chan.recv chan_A_Coordinator) 0 in
           let rec assent_reply_B =
             Marshal.from_string (Domainslib.Chan.recv chan_B_Coordinator) 0 in
           let rec assent_reply_C =
             Marshal.from_string (Domainslib.Chan.recv chan_C_Coordinator) 0 in
           let rec assent_reply_D =
             Marshal.from_string (Domainslib.Chan.recv chan_D_Coordinator) 0 in
           let rec assent_reply_E =
             Marshal.from_string (Domainslib.Chan.recv chan_E_Coordinator) 0 in
           let rec assent_reply_F =
             Marshal.from_string (Domainslib.Chan.recv chan_F_Coordinator) 0 in
           let rec assent_consensus =
             ((((assent_reply_A * assent_reply_B) * assent_reply_C) *
                 assent_reply_D)
                * assent_reply_E)
               * assent_reply_F in
           if assent_consensus = 1
           then
             (Domainslib.Chan.send chan_Coordinator_A "COMMIT";
              Domainslib.Chan.send chan_Coordinator_B "COMMIT";
              Domainslib.Chan.send chan_Coordinator_C "COMMIT";
              Domainslib.Chan.send chan_Coordinator_D "COMMIT";
              Domainslib.Chan.send chan_Coordinator_E "COMMIT";
              Domainslib.Chan.send chan_Coordinator_F "COMMIT";
              (let rec commit_reply_A =
                 Marshal.from_string
                   (Domainslib.Chan.recv chan_A_Coordinator) 0 in
               let rec commit_reply_B =
                 Marshal.from_string
                   (Domainslib.Chan.recv chan_B_Coordinator) 0 in
               let rec commit_reply_C =
                 Marshal.from_string
                   (Domainslib.Chan.recv chan_C_Coordinator) 0 in
               let rec commit_reply_D =
                 Marshal.from_string
                   (Domainslib.Chan.recv chan_D_Coordinator) 0 in
               let rec commit_reply_E =
                 Marshal.from_string
                   (Domainslib.Chan.recv chan_E_Coordinator) 0 in
               let rec commit_reply_F =
                 Marshal.from_string
                   (Domainslib.Chan.recv chan_F_Coordinator) 0 in
               ()))
           else
             (Domainslib.Chan.send chan_Coordinator_A "ABORT";
              Domainslib.Chan.send chan_Coordinator_B "ABORT";
              Domainslib.Chan.send chan_Coordinator_C "ABORT";
              Domainslib.Chan.send chan_Coordinator_D "ABORT";
              Domainslib.Chan.send chan_Coordinator_E "ABORT";
              Domainslib.Chan.send chan_Coordinator_F "ABORT";
              (let rec abort_reply_A =
                 Marshal.from_string
                   (Domainslib.Chan.recv chan_A_Coordinator) 0 in
               let rec abort_reply_B =
                 Marshal.from_string
                   (Domainslib.Chan.recv chan_B_Coordinator) 0 in
               let rec abort_reply_C =
                 Marshal.from_string
                   (Domainslib.Chan.recv chan_C_Coordinator) 0 in
               let rec abort_reply_D =
                 Marshal.from_string
                   (Domainslib.Chan.recv chan_D_Coordinator) 0 in
               let rec abort_reply_E =
                 Marshal.from_string
                   (Domainslib.Chan.recv chan_E_Coordinator) 0 in
               let rec abort_reply_F =
                 Marshal.from_string
                   (Domainslib.Chan.recv chan_F_Coordinator) 0 in
               ())) in
         let rec start_time = gettimeofday () in
         let rec _unit_34 = two_phase_commit in
         let rec end_time = gettimeofday () in
         let rec time_diff = (sub_float end_time) start_time in
         print_float time_diff)
  and domain_D =
    Domain.spawn
      (fun _ ->
         let rec gettimeofday arg = Unix.gettimeofday arg in
         let rec print_float arg = Stdlib.print_float arg in
         let rec sub_float arg = Stdlib.(-.) arg in
         let rec get_mod arg = Stdlib.(mod) arg in
         let rec assent_A = () in
         let rec assent_B = () in
         let rec assent_C = () in
         let rec assent_D =
           let rec x = 10 in
           let rec msg_ask =
             Marshal.from_string (Domainslib.Chan.recv chan_Coordinator_D) 0 in
           let rec msg_ask_intermed_result = (get_mod x) msg_ask in
           if msg_ask_intermed_result = 0
           then
             (Domainslib.Chan.send chan_D_Coordinator "L";
              (let rec msg_ask_res = 1 in msg_ask_res))
           else
             (Domainslib.Chan.send chan_D_Coordinator "R";
              (let rec msg_ask_res = 0 in msg_ask_res)) in
         let rec assent_E = () in
         let rec assent_F = () in
         let rec commit_A = () in
         let rec commit_B = () in
         let rec commit_C = () in
         let rec commit_D = let rec commit_val = 5 in commit_val in
         let rec commit_E = () in
         let rec commit_F = () in
         let rec abort_A = () in
         let rec abort_B = () in
         let rec abort_C = () in
         let rec abort_D = let rec abort_val = () in abort_val in
         let rec abort_E = () in
         let rec abort_F = () in
         let rec two_phase_commit =
           let rec assent_reply_D = assent_D in
           let rec _unit_40 =
             let val_39 = assent_reply_D in
             Domainslib.Chan.send chan_D_Coordinator
               (Marshal.to_string val_39 []) in
           match Domainslib.Chan.recv chan_Coordinator_D with
           | "COMMIT" ->
               let rec commit_val = commit_D in
               let rec _unit_36 =
                 let val_35 = commit_val in
                 Domainslib.Chan.send chan_D_Coordinator
                   (Marshal.to_string val_35 []) in
               ()
           | "ABORT" ->
               let rec abort_val = abort_D in
               let rec _unit_38 =
                 let val_37 = abort_val in
                 Domainslib.Chan.send chan_D_Coordinator
                   (Marshal.to_string val_37 []) in
               ()
           | _ -> failwith "Runtime Error: Unmatched label" in
         let rec _unit_41 = two_phase_commit in ())
  and domain_E =
    Domain.spawn
      (fun _ ->
         let rec gettimeofday arg = Unix.gettimeofday arg in
         let rec print_float arg = Stdlib.print_float arg in
         let rec sub_float arg = Stdlib.(-.) arg in
         let rec get_mod arg = Stdlib.(mod) arg in
         let rec assent_A = () in
         let rec assent_B = () in
         let rec assent_C = () in
         let rec assent_D = () in
         let rec assent_E =
           let rec x = 10 in
           let rec msg_ask =
             Marshal.from_string (Domainslib.Chan.recv chan_Coordinator_E) 0 in
           let rec msg_ask_intermed_result = (get_mod x) msg_ask in
           if msg_ask_intermed_result = 0
           then
             (Domainslib.Chan.send chan_E_Coordinator "L";
              (let rec msg_ask_res = 1 in msg_ask_res))
           else
             (Domainslib.Chan.send chan_E_Coordinator "R";
              (let rec msg_ask_res = 0 in msg_ask_res)) in
         let rec assent_F = () in
         let rec commit_A = () in
         let rec commit_B = () in
         let rec commit_C = () in
         let rec commit_D = () in
         let rec commit_E = let rec commit_val = 5 in commit_val in
         let rec commit_F = () in
         let rec abort_A = () in
         let rec abort_B = () in
         let rec abort_C = () in
         let rec abort_D = () in
         let rec abort_E = let rec abort_val = () in abort_val in
         let rec abort_F = () in
         let rec two_phase_commit =
           let rec assent_reply_E = assent_E in
           let rec _unit_47 =
             let val_46 = assent_reply_E in
             Domainslib.Chan.send chan_E_Coordinator
               (Marshal.to_string val_46 []) in
           match Domainslib.Chan.recv chan_Coordinator_E with
           | "COMMIT" ->
               let rec commit_val = commit_E in
               let rec _unit_43 =
                 let val_42 = commit_val in
                 Domainslib.Chan.send chan_E_Coordinator
                   (Marshal.to_string val_42 []) in
               ()
           | "ABORT" ->
               let rec abort_val = abort_E in
               let rec _unit_45 =
                 let val_44 = abort_val in
                 Domainslib.Chan.send chan_E_Coordinator
                   (Marshal.to_string val_44 []) in
               ()
           | _ -> failwith "Runtime Error: Unmatched label" in
         let rec _unit_48 = two_phase_commit in ())
  and domain_F =
    Domain.spawn
      (fun _ ->
         let rec gettimeofday arg = Unix.gettimeofday arg in
         let rec print_float arg = Stdlib.print_float arg in
         let rec sub_float arg = Stdlib.(-.) arg in
         let rec get_mod arg = Stdlib.(mod) arg in
         let rec assent_A = () in
         let rec assent_B = () in
         let rec assent_C = () in
         let rec assent_D = () in
         let rec assent_E = () in
         let rec assent_F =
           let rec x = 10 in
           let rec msg_ask =
             Marshal.from_string (Domainslib.Chan.recv chan_Coordinator_F) 0 in
           let rec msg_ask_intermed_result = (get_mod x) msg_ask in
           if msg_ask_intermed_result = 0
           then
             (Domainslib.Chan.send chan_F_Coordinator "L";
              (let rec msg_ask_res = 1 in msg_ask_res))
           else
             (Domainslib.Chan.send chan_F_Coordinator "R";
              (let rec msg_ask_res = 0 in msg_ask_res)) in
         let rec commit_A = () in
         let rec commit_B = () in
         let rec commit_C = () in
         let rec commit_D = () in
         let rec commit_E = () in
         let rec commit_F = let rec commit_val = 5 in commit_val in
         let rec abort_A = () in
         let rec abort_B = () in
         let rec abort_C = () in
         let rec abort_D = () in
         let rec abort_E = () in
         let rec abort_F = let rec abort_val = () in abort_val in
         let rec two_phase_commit =
           let rec assent_reply_F = assent_F in
           let rec _unit_54 =
             let val_53 = assent_reply_F in
             Domainslib.Chan.send chan_F_Coordinator
               (Marshal.to_string val_53 []) in
           match Domainslib.Chan.recv chan_Coordinator_F with
           | "COMMIT" ->
               let rec commit_val = commit_F in
               let rec _unit_50 =
                 let val_49 = commit_val in
                 Domainslib.Chan.send chan_F_Coordinator
                   (Marshal.to_string val_49 []) in
               ()
           | "ABORT" ->
               let rec abort_val = abort_F in
               let rec _unit_52 =
                 let val_51 = abort_val in
                 Domainslib.Chan.send chan_F_Coordinator
                   (Marshal.to_string val_51 []) in
               ()
           | _ -> failwith "Runtime Error: Unmatched label" in
         let rec _unit_55 = two_phase_commit in ()) in
  Domain.join domain_A;
  Domain.join domain_B;
  Domain.join domain_C;
  Domain.join domain_Coordinator;
  Domain.join domain_D;
  Domain.join domain_E;
  Domain.join domain_F
