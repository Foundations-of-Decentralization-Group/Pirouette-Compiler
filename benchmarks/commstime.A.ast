(
  (_) :=
    (allow E choice
     | R -> ()
     | L ->
         let
           (
             (successor) := (<~ C)
           )
         in
           (let (
                  (_) := ((ret (successor)) ~> B)
                ) in (())))
  (main) := (())
)
